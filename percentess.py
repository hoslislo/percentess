# The %s is known as a format specifier, specifically for strings. Specifiers act as a
# placeholder; after the string, the % precedes the values to be inserted into the format
# specifier.
print "John is %s%s" % ("every ", "Man")
# newer functionality
print "John is {}{}".format("every ", "Man")
